# wxautox4 拟人化操作 - 验证检查清单

## 功能实现验证
- [x] 检查 `utils/human.py` 是否存在且包含所有必要函数（human_sleep, human_move_to, human_click, human_type_text）
- [x] 检查 `human_sleep()` 函数是否支持正态分布随机延迟
- [x] 检查 `human_move_to()` 函数是否使用贝塞尔曲线实现平滑移动
- [x] 检查 `human_click()` 函数是否在控件范围内随机偏移点击位置
- [x] 检查 `human_type_text()` 函数是否支持逐字输入和随机按键间隔

## 参数配置验证
- [x] 检查 `param.py` 是否添加了拟人化相关配置项
- [x] 检查是否添加了拟人化功能开关配置
- [x] 检查 `LISTEN_INTERVAL` 是否修改为范围配置

## chatbox.py 修改验证
- [x] 检查 `send_text()` 方法是否支持 `mode` 参数
- [x] 检查短消息是否默认使用逐字输入模式
- [x] 检查长消息是否使用粘贴模式并添加随机延迟
- [x] 检查重试逻辑是否使用随机延迟

## sessionbox.py 修改验证
- [x] 检查 `search()` 方法是否添加了随机按键间隔
- [x] 检查 `switch_chat()` 方法是否使用平滑鼠标移动
- [x] 检查所有固定延迟是否替换为 `human_sleep()` 调用

## wx.py 修改验证
- [x] 检查所有硬编码 `time.sleep()` 是否替换为 `human_sleep()`
- [x] 检查监听循环是否添加了随机噪声行为
- [x] 检查导航切换方法是否添加了随机延迟

## 兼容性验证
- [x] 检查 `utils/__init__.py` 是否正确导出拟人化函数
- [x] 检查现有 API 接口是否保持向后兼容
- [x] 检查现有测试用例是否全部通过

## 拟人化效果验证
- [x] 观察鼠标移动轨迹是否平滑自然
- [x] 观察点击位置是否随机变化
- [x] 观察文本输入是否有自然的按键间隔
- [x] 观察操作延迟是否不规律且符合人类行为模式
- [x] 验证拟人化操作未显著降低性能（额外延迟 < 200ms/操作）

## 代码质量验证
- [x] 检查代码是否符合项目编码规范
- [x] 检查函数级注释是否完整
- [x] 检查是否有未使用的导入或变量
- [x] 检查异常处理是否完善
